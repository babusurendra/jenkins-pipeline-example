#!/usr/bin/env groovy

pipeline {
    agent any
   
    stages {
        stage('Build') {
             steps {
                echo "NodeJS application build is success"
           }
        }
    }

}
